/**
 * @type { Array<{ name: string, icon: string, category: string }> }
 */
export const SKILLS = [];

/**
 * @type { Array<{ name: string, description: string, image: string, link: string, skillset: Array<string> }> }
 */
export const PROJECTS = [];

/**
 * @type { Array<{ name: string, icon: string, link: string }> }
 */
export const SOCIALS = [];
